name: blocks-storage
runtime:
  name: python
  options:
    virtualenv: ../../.venv
    pythonpath: ../../src
description: Object Storage — one bucket of every storage spell (general-purpose, backup, data lake, archive, static website)
config:
  namespace:
    type: string
    description: OCI tenancy-level Object Storage namespace (visible in OCI Console under Tenancy Details)
